import React from "react";

/**
 * Note: Hero trust benefits are now integrated directly inside the HeroSection
 * at the bottom of the left content column, exactly matching the reference design.
 * This component returns null to prevent duplicate rendering.
 */
export default function HeroTrustStrip() {
  return null;
}
